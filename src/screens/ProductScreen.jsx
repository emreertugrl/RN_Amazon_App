import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import CustomText from '../CustomText';

const ProductScreen = () => {
  return (
    <View>
      <CustomText>ProductScreen</CustomText>
    </View>
  );
};

export default ProductScreen;

const styles = StyleSheet.create({});
